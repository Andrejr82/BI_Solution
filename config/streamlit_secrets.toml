# ===================================================================
#  Segredos para o Streamlit Cloud
#
#  Copie e cole TODO o conteúdo deste arquivo na caixa de texto
#  de "Secrets" nas configurações do seu aplicativo no Streamlit.
#
#  IMPORTANTE: O sistema agora usa APENAS Gemini e DeepSeek.
#  OpenAI foi completamente removido do código.
# ===================================================================

# -----------------------------------------
# Chaves de API para os Modelos de Linguagem
# -----------------------------------------

# Chave da API do Google Gemini (LLM Principal)
# Substitua pelo valor real da sua chave
GEMINI_API_KEY = "AIzaSy..."

# Chave da API do DeepSeek (LLM de Fallback)
# Substitua pelo valor real da sua chave
DEEPSEEK_API_KEY = "sk-af1b..."


# -----------------------------------------
# Modelo de Linguagem Principal
# -----------------------------------------
# Define qual modelo o agente usará.
LLM_MODEL_NAME = "gemini-2.5-flash"


# -----------------------------------------
# Configurações do Banco de Dados SQL Server
# -----------------------------------------
# O nome do servidor do seu banco de dados
DB_SERVER = "seu_servidor.database.windows.net"

# O nome do seu banco de dados
DB_NAME = "Projeto_Caculinha"

# Seu nome de usuário para o banco de dados
DB_USER = "AgenteVirtual"

# Sua senha para o banco de dados
DB_PASSWORD = "sua_senha_aqui"

# O driver ODBC para a conexão
DB_DRIVER = "ODBC Driver 17 for SQL Server"

# Confiar no certificado do servidor (geralmente "yes" para Azure SQL)
DB_TRUST_SERVER_CERTIFICATE = "yes"
