# Arquivos Deprecated

Arquivos movidos para esta pasta em 2025-10-25 devido a imports quebrados:

## Arquivos Arquivados:

1. **check_mcp_online.py** - Import: `core.tools.node_mcp_client` (não existe)
2. **evaluate_agent.py** - Import: `core.query_processor` (não existe)
3. **generate_db_html.py** - Import: `core.tools.node_mcp_client` (não existe)
4. **setup_database_optimization.py** - Import: `core.database.sqlalchemy_connector` (não existe)
5. **test_fix_memory_errors.py** - Import: `core.config.config` (não existe)

## Motivo:

Estes arquivos provavelmente são de versões anteriores do projeto e não foram atualizados.
Os módulos que eles importam não existem mais no projeto atual.

## Ação Recomendada:

- Se precisar desses scripts, atualize os imports para os módulos corretos
- Caso contrário, podem ser deletados após revisão
