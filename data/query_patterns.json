{
  "ranking_completo_por_segmento": {
    "description": "Gera um ranking completo de vendas de produtos para um segmento específico, sem limitar o número de resultados.",
    "keywords": ["ranking", "completo", "todos", "segmento"],
    "examples": [
      {
        "user_query": "ranking de vendas completo do segmento tecidos",
        "code": "df = load_data()\ndf_filtered = df[df['nomesegmento'].str.upper() == 'TECIDOS']\nranking = df_filtered.groupby('nome_produto')['vendas_total'].sum().sort_values(ascending=False).reset_index()\nresult = ranking"
      }
    ]
  },
  "top_n_geral": {
    "description": "Gera um ranking limitado (Top N) de produtos mais vendidos no geral.",
    "keywords": ["top", "mais vendidos", "maiores", "ranking"],
    "examples": [
      {
        "user_query": "quais são os 10 produtos mais vendidos?",
        "code": "df = load_data()\nranking = df.groupby('nome_produto')['vendas_total'].sum().sort_values(ascending=False).reset_index()\nresult = ranking.head(10)"
      },
      {
        "user_query": "top 5 produtos",
        "code": "df = load_data()\nranking = df.groupby('nome_produto')['vendas_total'].sum().sort_values(ascending=False).reset_index()\nresult = ranking.head(5)"
      }
    ]
  },
  "top_n_por_une": {
    "description": "Gera um ranking limitado (Top N) dos produtos mais vendidos para uma UNE específica.",
    "keywords": ["top", "mais vendidos", "maiores", "ranking", "une"],
    "examples": [
      {
        "user_query": "top 10 produtos mais vendidos na une 261",
        "code": "df = load_data()\ndf_filtered = df[df['une_nome'] == '261']\nranking = df_filtered.groupby('nome_produto')['vendas_total'].sum().sort_values(ascending=False).reset_index()\nresult = ranking.head(10)"
      }
    ]
  }
}